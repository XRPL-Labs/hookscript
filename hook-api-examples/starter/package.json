{
  "name": "starter",
  "version": "1.0.0",
  "description": "Starter is the simplest possible hook: it just accepts all transactions and logs that it is running.",
  "main": "starter.js",
  "scripts": {
    "compile": "../../assemblyscript/bin/asc.js --disable=bulk-memory --runtime=stub starter.ts -O3 -o starter.wasm && hook-cleaner starter.wasm"
  },
  "author": "Equilibrium - https://equilibrium.co",
  "license": "Public Domain",
  "dependencies": {
    "ripple-keypairs": "^1.1.4",
    "xrpl-accountlib": "^1.6.1",
    "xrpl-client": "^2.0.1"
  }
}
